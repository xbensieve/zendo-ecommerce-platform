# Test profile — datasource provided by Testcontainers
# Activated by @ActiveProfiles("test")

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

  flyway:
    enabled: true
